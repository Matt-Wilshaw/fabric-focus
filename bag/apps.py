"""App configuration for the Bag app."""

from django.apps import AppConfig


class BagConfig(AppConfig):
    """Django app configuration for the Bag app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'bag'
